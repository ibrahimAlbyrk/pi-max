---
name: tools/write-short
description: Short description for the write tool (used in system prompt tool list)
version: 1
---
Write content to a file. Creates the file if it doesn't exist, overwrites if it does. Automatically creates parent directories. Use only for new files or complete rewrites — prefer edit for partial changes.